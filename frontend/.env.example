# Local: set by ./start-all.sh. For manual deploy: npm run deploy:local then paste address here.
VITE_CONTRACT_ADDRESS=
# Leave empty for local. Only needed for Sepolia (fixes read errors on Verify/My Documents).
# VITE_SEPOLIA_RPC_URL=
# Leave empty to use Vite dev server proxy to backend
VITE_API_URL=